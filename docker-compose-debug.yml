version: '3.8'
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: banking_backend_debug
    environment:
      NODE_ENV: production
      PORT: 5000
    stdin_open: true
    tty: true
    command: ["sh"]
