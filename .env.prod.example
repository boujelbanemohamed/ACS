# ===========================================
# CONFIGURATION PRODUCTION - ACS BANKING
# ===========================================
# Copiez ce fichier vers .env.prod et modifiez les valeurs

# Base de données
DB_NAME=banking_db
DB_USER=banking_user
DB_PASSWORD=VotreMotDePasseSecurise123!

# JWT - OBLIGATOIRE: Générez avec: openssl rand -base64 64
JWT_SECRET=REMPLACEZ_PAR_UNE_CLE_GENEREE_AVEC_OPENSSL
JWT_EXPIRE=8h

# Timezone
TZ=Africa/Tunis

# Cron Schedule (scan automatique)
CRON_SCHEDULE=*/5 * * * *

# URL API pour le frontend
API_URL=http://votre-domaine.com:8000/api
